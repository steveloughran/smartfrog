

#include "org/smartfrog/services/anubisdeployer/components.sf"

sfConfig extends ClusterSystem {
    nodeDescription extends DATA {
        resources extends DATA {
            serviceInstances 2;  // can run a single instance of a service
                                 // ensures that at most one webserver or manager runs anywhere
        }
        attributes extends DATA {}
        data extends DATA {}
    }
    anubisAddress "233.1.1.56";
}