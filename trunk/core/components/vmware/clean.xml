<?xml version="1.0"?>

<project name="clean" default="cleanArtifacts">

    <description>
        One-step target for lazy ones.
    </description>

    <property name="user.home" value="C:/Documents and Settings/mahrt.EMEA"/>
	<property name="artifact.name" value="sf-vmware" />
	
    <property name="ivy.smartfrog.dir" value="${user.home}/.ivy/cache/org.smartfrog" />
    <property name="ivy2.smartfrog.local.dir" value="${user.home}/.ivy2/local/org.smartfrog" />
    <property name="ivy2.smartfrog.cache.dir" value="${user.home}/.ivy2/cache/org.smartfrog" />

    <target name="cleanArtifacts">
        <delete dir="${ivy.smartfrog.dir}/${artifact.name}" />
        <delete dir="${ivy2.smartfrog.local.dir}/${artifact.name}" />
        <delete dir="${ivy2.smartfrog.cache.dir}/${artifact.name}" />
    </target>
</project>