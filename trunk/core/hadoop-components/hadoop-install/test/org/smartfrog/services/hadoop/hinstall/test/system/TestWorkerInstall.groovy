package org.smartfrog.services.hadoop.hinstall.test.system

import org.smartfrog.services.scripting.groovy.GroovyTestBase

/**
 *
 */
class TestWorkerInstall extends GroovyTestBase {

    final String PACKAGE = "/org/smartfrog/services/hadoop/hinstall/test/system/"
    /**
     * This test verifies that the worker can be installed without having to put in the files
     */
    
    void testWorkerFiles() {
     //   expectSuccessfulTestRunOrSkip(PACKAGE, "")
    }
    
}
